services:
  service-webhook-blue:
    image: ${SERVICE_WEBHOOK_IMAGE:?set SERVICE_WEBHOOK_IMAGE}:${SERVICE_WEBHOOK_VERSION:?set SERVICE_WEBHOOK_VERSION}

  service-webhook-green:
    image: ${SERVICE_WEBHOOK_IMAGE:?set SERVICE_WEBHOOK_IMAGE}:${SERVICE_WEBHOOK_VERSION:?set SERVICE_WEBHOOK_VERSION}
